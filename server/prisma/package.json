{
  "name": "@etheriatimes/prisma",
  "version": "1.0.0",
  "description": "Prisma schema and database layer for The Etheria Times",
  "private": true,
  "main": "schema.prisma",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "prisma db seed",
    "db:push": "prisma db push",
    "db:reset": "prisma migrate reset --force"
  },
  "devDependencies": {
    "@prisma/client": "^7.4.2",
    "prisma": "^7.5.0"
  }
}
